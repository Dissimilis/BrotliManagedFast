version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
  - package-ecosystem: nuget
    directory: /src
    schedule:
      interval: monthly
    ignore:
      # The competitor package is frozen upstream; keep the benchmarked version stable.
      - dependency-name: BrotliSharpLib
